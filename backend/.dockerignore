__pycache__
*.pyc
staticfiles
.env
.env.*
.git
*.md

# Test / dev artifacts (build output only — not source files)
# Note: conftest.py, */tests/, and requirements-dev.txt are NOT excluded
# because the dev Dockerfile stage needs them at build time.
# .dockerignore applies to the entire build context, not per-stage.
.coverage
.pytest_cache/
.ruff_cache/
htmlcov/

# IDE / OS
.vscode/
.idea/
.DS_Store
