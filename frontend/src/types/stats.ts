export interface DailyStats {
  hours_focused_today: number;
  blocks_completed_today: number;
  blocks_total_today: number;
  current_streak: number;
  weekly_work_hours: number;
  weekly_study_hours: number;
}
